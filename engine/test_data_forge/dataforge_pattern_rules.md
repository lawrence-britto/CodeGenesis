# Data Forge Pattern Rules

Deterministic precedence: blank, constant, fixed/variable masks, numeric, date, semantic shape, then column-name semantic hints. Composite primary-key capacity is enforced before generation. Values are generated from patterns and ranges, never copied as complete sample rows.
